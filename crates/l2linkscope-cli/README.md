# l2linkscope

Command-line interface for L2LinkScope.

Current status: placeholder only. The executable supports `--help` and
`--version` so the workspace can build and future CLI work has a package
boundary.

No discovery commands are implemented yet, and the CLI does not require
privileges in this scaffold.
